# CLAUDE.md — このプロジェクトでのAIアシスタント設定

> **新しいセッション開始時に必ずこのファイルを読むこと。**
> 運用マニュアル全体は `docs/playbook.md` を参照。

---

## 🚨 自動実行は絶対に自分から仕込まない（2026-09-03 ユーザー指示・重大ミス）

**ユーザーの明示的な指示がない限り、定期実行・自動監視を一切登録しない。**

過去にやってしまったこと：PR を作成したあと、自分の判断で「1時間おきに PR の状態を確認する」
Routine と PR イベント購読を登録した。状態は何も変わらないのに毎時発火し、同じ「変化なし」報告を
繰り返してトークンを浪費した。ユーザーから停止指示を受けて全解除済み。**二度と繰り返さない。**

### 禁止すること（ユーザーがそう言わない限り）

- Routine / cron / スケジュールトリガーの作成（`create_trigger`・`send_later`・`CronCreate` 等）
- PR や Issue のイベント購読（`subscribe_pr_activity` 等）
- 自分への「あとで確認する」リマインダーの登録
- ポーリング目的のループ・待機

「PR を作ったから監視しておこう」「念のため後で見に行こう」という**善意の先回りが最も危険**。
やるべきは、作業を終えたら報告して黙って待つこと。続きが必要ならユーザーが声をかける。

### 許可されるケース

ユーザーが「監視して」「定期的に実行して」「○分おきに確認して」と**自分の言葉で明示的に依頼した時だけ**。
その場合も、いつ・何を・どの頻度で実行するかを事前に伝えてから登録する。

---

## GA4 MCP 接続について

このプロジェクトでは **GA4 MCP ツールが自動的に利用可能**です。

**セッション開始時の手順（必須）:**

```
ToolSearch で "run_report" を検索 → mcp__*__run_report ツールを取得
```

- 認証情報（サービスアカウントJSON、gcloud ADC、API Key）は**不要**
- Python SDK、curl、gcloud コマンドは**使わない**
- MCP ツール名は `mcp__<UUID>__run_report` 形式（UUIDはセッションごとに変わる場合がある）

> ⭐ **起動環境は毎回確認しない（2026-06-08 ユーザー指示）**：ユーザーは GA4 を使わせたい時、必ず Desktop アプリから指示している。だから「この環境で引けますか？」と先回りで聞かず、**まず `run_report` を1発打って疎通を確認 → 繋がれば黙ってそのまま進める**。応答しない／取得できない時だけ確認する。詳細は `docs/playbook.md` Section 1.1。

**正しい呼び出し例:**

```json
{
  "property_id": "347074845",
  "date_ranges": [{"start_date": "2026-03-09", "end_date": "2026-04-05"}],
  "dimensions": [],
  "metrics": ["sessions", "activeUsers"]
}
```

**よくある失敗パターン（やってはいけないこと）:**

- `python3 -c "from google.analytics.data_v1beta import ..."` → SDK未認証でエラー
- `gcloud auth application-default login` → gcloudは未インストール
- 「認証情報を貼り付けてください」とユーザーに聞く → 不要、MCPで直接接続可能

---

## 基本情報

- **GA4 Property ID**: `347074845`
- **Vercel URL**: https://v2-veltra-cvr.vercel.app/
- **開発ブランチ規則**: `claude/<task>-<hash>` 形式で作業し、完了後 push
- **サイト構成（2026 H2〜／大改編）**: トップ `index.html` は Northstar 振り分けhub。これまでの全ページ（CVRレポート・分析・施策）は **`2026h1/` 配下**、H2 ミッションは **`2026h2/index.html`**。共有アセット（`auth.js`/`nav.js`/`funnel-def.js`/`summary-detail.js`/各CSS/各JSON）と `login.html`・`index.html` はルート据え置き。
- **週次レポートの配置**: 週ディレクトリは **`2026h1/reports/{YYYY}-{h1|h2}/{YYYY}-w{WW}/`**（上半期 W1〜W26 → `h1`、下半期 W27〜 → `h2`）。公開URLは `/2026h1/reports/{YYYY}-{h1|h2}/{YYYY}-w{WW}/`。本書中の `reports/{W}/…` はすべて `2026h1/reports/` 配下を指す。W番号からのパス変換は `week_reldir()`（Python）/ `weekDirFor()`（JS）が担当。

---

## よく使うクエリパターン

### ベースラインファネル（28日間）
```python
# dimensions=[], metrics=["sessions","activeUsers","purchaseRevenue"]
# dimension_filter で pagePath/eventName を切り替えて各ステップ取得
```

### エリア別（landingPage ディメンション）
```python
# dimensions=["landingPage"], limit=10000
# Python で area_key に集計後 data.json に格納
```

詳細な手順は `docs/playbook.md` のセクション4「Phase 1: データ取得」を参照。

---

## KPIダッシュボード（/kpi.html）更新漏れ防止

週次更新時にまず `summary-data.json` の `monthly` 配列を当月値で上書きする（他ファイルと同じ Phase 1 ステップ）。加えて **KPI ダッシュボード固有の更新**は以下：

| 頻度 | ファイル | 対象 |
|------|---------|------|
| 週次 | `summary-data.json` | `monthly[].{sessions,purchases,cvr}` — 当月速報 |
| **月次確定時** | `summary-data.json` | `monthly_notes["YYYY-MM"].{summary,good,bad}` — 前月の分析コメント |
| **四半期確定時** | `summary-data.json` | `targets.{baseline_cvr, monthly_cvr, annual_cvr, stretch_cvr, stretch_revenue*}` を再計算 |
| **四半期確定時** | `kpi.html` | arrow-row の 3 つのハードコード CVR 表示値（`biz-cvr-current / target / stretch`）+ 進捗ラベル + stretch-note |

完全な手順・分析コメントのテンプレート・計算式は `docs/playbook.md` セクション11「KPIダッシュボード（/kpi.html）の更新」を参照。

---

## ボトルネックのタイトル作成ルール（Phase 1 / Phase 2 共通）

`data.json` と `bottleneck-N-content.json` のタイトルは **「誰が・どこで・どう困っているか」を1文で描く課題目線**で書く。数値（%・pp）は `tags` / `deviation` / `description_html` が担当するのでタイトル枠では繰り返さない。

❌ NG: `新規ユーザーの意向→完了率が 51.1%（全体 76.9% から -25.9pp）`
✅ OK: `新規ユーザーが『長いフォーム＋最終価格の不意打ち』で決済直前に諦めている`

タイトル書き換えは **Step 1.5（behavior_context）完了後に Step 1.6 として必ず実施**し、以下3箇所を同時に更新する：
- `reports/{W}/data.json` の `bottlenecks[].title`
- `reports/{W}/bottleneck-N-content.json` の `title`
- `reports/{W}/index.html`（Phase 2.9 の週次サマリー）の Action 2 Top3 `.asl-title` ハードコード箇所

詳細ルールは `docs/playbook.md` Phase 2 Step 1.6 を参照。

---

## YoY/前年比較の作法（過小評価・誤読を防ぐ）

CVR / セッション等の年次比較を書くときに**必ず守る 3 原則**:

### 1. matched 同期で比較する（通年平均は使わない）

❌ NG: `2026 通年平均 1.27%` ← 2026 が Jan-May しかない場合、7-8月（高 CVR 月）が含まれないため過小評価される
✅ OK: `2026 Jan-May 1.331% vs 2025 Jan-May 1.542%`（同期同月数の volume-weighted CVR で比較）

### 2. ベルトラの本当のピーク月は 7-8月 と 12-1月

| 区分 | 月 | 特性 |
|------|-----|------|
| 夏休み・お盆 | **7-8月** | 年間最重要繁忙期、CVR 1.48〜1.54%（2024-25 実績） |
| 年末年始 | **12-1月** | 第二繁忙期、CVR 1.40〜1.61%（2024 12 月が 1.614% で年間最高） |
| 中位需要 | 4-6月、9-10月 | 通常水準、CVR 1.40〜1.55% |
| 谷 | 2-3月、11月 | やや低め、CVR 1.35〜1.50% |
| 最弱 | 5月（GW 後半は実質予約閑散） | 1.10〜1.20% 帯（2024 1.177%） |

通年平均で「2025 が peak year」と言いがちだが、<strong>繁忙期で見ると 2025-12 で既に -0.20pp 崩壊が始まっていた</strong>。「平均が良い年」 ≠ 「全部良い年」。

### 3. 「2026 はまだ来ていない月」を考慮する

- 2026 5月時点で「2026 全体が 2024 水準まで後退」と断じるのは早すぎる
- 残り 7-12月（うち 7-8月は最重要繁忙期）で挽回可能か / さらに崩れるかが運命の分岐点
- 結論は「(matched) 同期で見ると -0.XXpp、繁忙期の test はまだ来ていない」とニュアンス付き

### 適用先

スポット分析（`spot/2026-gw-cvr-decline.html` 等）、W サマリー（`reports/{W}/index.html`）、KPI ダッシュボード（`kpi.html`）、月次 notes（`summary-data.json` の `monthly_notes`）すべてでこの 3 原則を守る。

---

## Figma実装ルール（絶対守る）

**「Figmaの通りに」「このデザインで」「添付に合わせて」と指示されたら、必ずFigma MCPで実値を引いてから実装する。「それっぽく」「常識的に」で当てるのは禁止。**

### 失敗パターン（2026-06-24 に発生、二度と繰り返さない）

野球モックのAC商品カードを作った時、ページ全体のデザイントークン（`--pink`, `--ink`等）は合わせたが、**「Card」コンポーネント（nodeId 520:21531）自体のスペックを引かずに**実装した。結果：

| 項目 | 実装した値（"それっぽく"） | Figma実値 |
|---|---|---|
| border-radius | 8px | **12px** |
| border | `--line-soft` | **`--line` (#DADADA)** |
| body padding | 12px | **12px 16px 16px 16px** |
| title font-weight | 400 | **700 (Bold)** |
| title line-clamp | 3行 | **2行** |
| rating score 色 | `--ink`（黒） | **`--pink` (#E4336B)** |
| price 色 | `--pink`（ピンク） | **`--ink` (#484848)** |
| price font-size | 16px | **14px** |

特に「価格はピンクで目立たせる」は **EC系の常識的な思い込み**。Figmaでは「価格は黒、レビュースコアをピンクで主役化」という意図的な設計だった。常識で当てると意図を破壊する。

### ルール

1. **コンポーネント単位でFigmaを引く**
   ページ全体のトークンを揃えただけでは不十分。カード/モーダル/ボタン等の独立コンポーネントは、**必ずnodeIdを取得し `get_design_context` で実値を取る**。ページ参照だけで済ませない。

2. **手順を必ず踏む**
   - Step 1: nodeIdをユーザーから受け取る or Figma URLから抽出
   - Step 2: `get_design_context` でCSS/トークン/スペーシング/フォントを取得
   - Step 3: 既存コードと **項目単位で差分表を作る**（上記のような表）
   - Step 4: 差分を1つずつ潰す
   - Step 5: 全項目一致を目視確認してから完了報告

3. **「常識」で値を埋めない**
   border-radius、padding、font-weight、色、line-height、line-clamp — すべてFigmaに実値がある。"よくあるカードのデザイン" を流用しない。「ECだから価格はピンク」のような業界常識も入れない。Figmaの設計意図を尊重する。

4. **報告の言い回し**
   「Figmaに合わせました」と言う時は、**項目単位の差分が全部潰れている**ことを意味する。1〜2箇所だけ直して「整えました」はNG。

### 適用範囲

`planning/sports-mock/**`、`planning/hawaii/**` 等のモックページ、施策ページ、ダッシュボード等、Figmaがソースとなるすべての実装。

---

## 施策ドキュメント執筆ルール（絶対守る）

`planning/` 配下の施策案 HTML、A/B Test Design Document、PRD などを書くときの必須ルール。
今までに「ユーザーから差し戻しを受けた表現・構造」をパターン化したもの。**繰り返さないこと**。

### 1. 「やり取りの痕跡」を成果物に残さない

ユーザーとの会話で出た気付きや改善ジャーニーをドキュメントに書き込まない。
読み手は「私たちのやり取り」を知らない。

**❌ 禁止例：**
- 「実物表示に拡張」「実物が見えない」（"実物" は会話で出た造語）
- 「AsIs vs TOBE — 改善後！」のような改善ボースト
- 「❌ カウントのみで意味がない／✅ 実物表示で良くなった」型の自己評価コメント
- 「v3 で全面更新」「今回上書き」のような版数追跡コメント
- footer の "企画書 v5.0" のような版数番号

**✅ OK：**
- ロジックの説明（「3 条件 AND で月次バッチ自動選定」など仕組み）
- 仕組みの説明（mechanism）
- 必要がある場合に限り、ガイドラインに沿った中立な before/after（装飾なし）

### 2. AsIs / TOBE を使うときの条件

原則として **TOBE（提案デザイン）だけ書く**。AsIs を入れるのは「現状を知らない読み手が
いる」ケースに限る。装飾的な ❌/✅ コメント・「改善後！」ボーストは付けない。

### 3. 「マーケが〜」「担当が〜」など人に責任を投げる表現は禁止

仕組み（ルール、バッチ、自動化）で書く。誰かに丸投げする文体は使わない。

**❌ NG：**「マーケが事前にエリアごとに手で選んだ固定リスト」
**✅ OK：**「3 条件 AND の月次バッチ自動選定」＋ 具体的なデータソース・クエリ・実行環境

### 4. 抽象ラベルだけで終わらせない

「緊急性」「個人化」「文脈依存」のような形容詞だけで並べ順や挙動を説明しない。
発火条件と意味を具体的に書く。

**❌ NG：**「① C（緊急性・購入直前）→ ② A（最も個人化）」
**✅ OK：**「① C：カレンダー開いて 48h 未購入＝もうすぐ買いそうシグナルが最強。最上部に置けば取りこぼし回収」

### 5. ツールジャーゴンに依存しない

「Braze の Content Cards priority 設定で制御」だけ書いても、Braze を知らない読み手には
何も伝わらない。仕組み（priority 整数を振る、SDK が高い順に並べる、ダッシュボードで
切替可能）まで書く。

### 6. 用語の統一

| 項目 | 決定事項 |
|---|---|
| サービス名 | **Veltra**（VELTRA / VeL·ToA 等の表記揺れ禁止） |
| ユーザー主語 | **Traveler**（「本人」「ユーザー」「お客様」NG） |
| 最小フォント | 13px 以上（モックアップ内部の UI 再現は除く） |
| デザイン | ライトテーマ（白背景）。コンサル風・ダーク風 NG |

### 7. 「擬似レコメンド」を扱う施策の必須セクション

Braze 等で「個人最適化のように見える表示」を作る場合、以下を**全部書く**こと（抽象論で止めない）：

1. **データソース（具体名）** — 例：GA4 BigQuery export `ga4_events_*` ＋ AC マスター
2. **生成クエリ実例** — SQL/Python の擬似コードを最低1つ載せる
3. **実行環境とスケジュール** — Cloud Scheduler / Cloud Functions 等
4. **Braze への投入経路** — REST API／手動 import／Connected Content のどれか明示
5. **カスタム属性スキーマ** — `last_viewed_ac_1_*` のような属性一覧表
6. **属性更新の仕組み** — Braze の Workflow / Canvas のどのアクションでスライド更新するか
7. **Liquid テンプレート実例** — 1 カード分は実コードで貼る
8. **稼働責任** — 「マーケが」ではなく「初期セットアップはエンジニア、以降はマネージドサービスで自走」のような仕組みベースの記述

### 8. Phase 分割するときのルール

Phase を分割するなら、以下のどれかを明示できること：
- ✅ **学習目的**：エリア粒度で先に効果検証してからスポット粒度に拡張
- ✅ **リスク管理**：マネジメント承認の段階制
- ✅ **依存解消**：他施策のリリース待ち

→ どれも当てはまらないなら**分割しない**。「とりあえず Phase 1 / 2」で書かない。
特に「開発作業 0 で Braze 入れるだけ」ならスケジュールを Week 単位で分けない。Day 単位で
コンパクトに書く。データ蓄積待ちは静的フォールバックで先行スタートし、待ち時間ゼロを実現する。

### 9. 構造（順番厳守）

1. **Executive Summary**（結論先出し）— モックや具体案を最初に見せる
2. **裏付けデータ・ロジック** — GA4 数値、選定基準、計算式
3. **スケジュール** — 開発工数、運用工数
4. **なぜやるか** — 背景・問題は最後（重要だが結論の後）

### 10. 提出前セルフチェック

- [ ] 「私たちの改善ジャーニー」が書かれてないか
- [ ] ❌/✅ ボースト callout が混ざってないか
- [ ] 「実物」「事前キュレーション」のような会話由来の語が残ってないか
- [ ] AsIs を載せるなら理由があるか（無いなら TOBE のみ）
- [ ] 「マーケが〜」「担当が〜」など責任投げ表現がないか
- [ ] 抽象ラベルだけで終わらせていないか（発火条件と意味が書かれているか）
- [ ] ツールジャーゴンに説明が付いているか
- [ ] サービス名は **Veltra**、主語は **Traveler** に統一されているか
- [ ] 数値・KPI に出典（GA4 期間や Property ID）が付いているか
- [ ] フォントサイズが 13px 以上か
- [ ] 戦略3軸（メインスポット取りこぼし防止／迷わず買う／滞在中プラスアルファ）のどれに該当するかが書かれているか
- [ ] 順位を「1位/2位/3位」のような誤読しやすい語ではなく「上から1番目」など明確に
- [ ] 擬似レコメンド施策なら「How の具体（データソース／クエリ／環境／投入経路／属性／テンプレ）」が全部書かれているか
